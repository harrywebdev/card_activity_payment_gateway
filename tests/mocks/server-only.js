// Vitest stub for the `server-only` package. In real Node code its default
// export throws on import to prevent it from being bundled into client code;
// inside the test runner that protection is irrelevant.
module.exports = {};
